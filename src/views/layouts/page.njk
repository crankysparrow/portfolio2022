{% extends './base.njk' %}

{% block baseContent %}
    <main id="main">

        <div class="wrapper">

            {% block title %}

                <div class='title-wrap'>
                    <h1>{{ title }}</h1>
                    {% if subtitle %}
                        <div class='description callout'>{{ subtitle }}</div>
                    {% endif %}
                </div>
            {% endblock %}

            {% block content %}

                {{ content | safe }}

            {% endblock %}

        </div>

    </main>
{% endblock %}